<h3>new article</h3>

<%= form_with(model: @article, local: true) do |form| %>
    <div>
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>
    <br>
    <%= submit_tag("Submit") %>
<% end %>
